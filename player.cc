#include <cstdlib>
#include <iostream>
using namespace std;
void give(Property &prop,Player &p){
	p.receive(prop);
	this.remove(prop);
}
void receive(Property &prop){
	this.add(prop);
}
void buyImproveProp(string propname){
	//find the prop under the array
	properties[i].buyImprove();
}
void sellImproveProp(string propname){
	properties[i].sellImprove();
}

void roll(){
	
}
void showAssets(){
	for loop
		cout<<;
}
bool isBankrupted(){
	if(debt>money){
		return true;
	}
	return false;
}
ostream & operator<<(ostream &out, Player* p){
	out<<"Player piece: "<<p.piece<<endl;
	out<<"Player's current cash: "<<p.money<<endl;
	return out;
}
